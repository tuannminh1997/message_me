<div class = "ui container">
  <% flash.each do |type, msg| %>
    <div class="ui <%= "#{type}" %> message">
      <i class="close icon"></i>
      <div class="header">
        <%= msg %>
      </div>
    </div>
  <% end %>
</div>


